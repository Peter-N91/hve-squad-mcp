---
bump: patch
type: Changed
---

- **Bumped the bundled cast pin from `0.12.8` to `Peter-N91/hve-squad@0.12.9`** and refreshed `host/cast/` from that release's `apm.yml`. This tracks a patch release upstream, so it carries the same level here.
