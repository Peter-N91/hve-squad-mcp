---
bump: patch
type: Changed
---

- **Bumped the bundled cast pin from `0.12.7` to `Peter-N91/hve-squad@0.12.8`** and refreshed `host/cast/` from that release's `apm.yml`. A cast change is a patch: the squad a consumer gets is different.
